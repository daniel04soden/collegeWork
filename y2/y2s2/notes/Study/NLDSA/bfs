marked = [false,false,false]

def bfs(g,r){
    q = createQ(sizeG)
    q.append(r)
    while q.size>0{
        u = q.pop(0)
        visit(u)
        marked[u] = true
        for w in u.neighbours
            if w.marked != true
                q.append(w)
    }
}
