#include <stdio.h>
#include <stdlib.h>
#define Y 12
int duplicate(const int *array, int len, int *copy) { array[0] = 1; }
int main() {
  short int myInt = 1;
  char myChar = 'c';
  unsigned int myUnSigned = -1;
  printf("%d", myUnSigned);

  unsigned int idx;
  int length = 20;
  for (idx = length - 1; idx > 0; idx--) {
    printf("Hi");
  }

  int ourInt = 0;
  char c = 'c';

  while (myInt >= 0) {
    char c = getchar();
    printf("%c\n", c);
    break;
  }
  printf("%c\n", c);

  const int x = 2;

  int array[10] = {1, 2, 4};

  int *dup = malloc(10 * sizeof(int));

  return 0;
}
