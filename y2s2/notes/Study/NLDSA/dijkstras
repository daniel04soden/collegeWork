def dijkstras(G,w,s){
    q = createQ()
    for vertices in q: // init shortest path
        q.weight = infintity
        q.append(v)
    while Q!=0
        u = d[u] > min(d[y])
        Q = Q - u
        for each neighbour u:
            relax(u,v,w)
}
